on:
  push:
    branches:
      - master






jobs:
  build:
    runs-on: ubuntu-latest
    
    steps:
      - name: 'Testing GitHub Actions'
        run: pwd

      - name: 'Testing printing'
        run: echo "dalia"
